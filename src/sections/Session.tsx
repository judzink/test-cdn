import styled from "styled-components"

const SessionWrapper = styled.div`
	background: white;
	width: 320px;
	border-radius: 8px;
	padding: 12px;
`

const Session = () => {
	return <SessionWrapper>Active Session</SessionWrapper>
}

export default Session
